---
context: default
phase: none
task: null
total_tasks: null
status: paused
last_updated: 2026-04-29
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_이번 세션에서 발견된 블로킹 제약 없음. 아래 섹션 참고._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Rive 빌드 에러 | rive_common 0.4.15가 안드로이드 네이티브 빌드에서 `app.rive.rive.RivePlugin` 미해결 오류 발생 | advisory | pubspec.yaml에서 rive 패키지 제거 완료. 재추가 금지. |

<current_state>
이번 세션에서 UI 개선 4건 완료 후 커밋·푸시까지 마쳤음.
현재 `origin/master`는 최신 상태 (커밋 `95936fa`).
앱은 빌드 성공 상태 (`app-debug.apk` 생성됨).
</current_state>

<completed_work>

이번 세션 완료 작업:
1. **텍스트 줄바꿈 수정** — `GoogleFonts.courierPrime` (한국어 미지원) → 기본 `TextStyle`로 교체 (`conversation_panel.dart`)
2. **채팅창 레이아웃 변경** — 좌우 `ResizableSplitView` → 세로 `Column` (이미지 38% / 채팅 62%) (`canvas_screen.dart`)
3. **AI 답변 길이 제한** — 시스템 프롬프트에 "2-3문장 이내" 규칙 추가 (`alien_repository.dart`)
4. **그림 전송 버그 수정** — `renderBox` null 시 `MediaQuery.size`로 폴백, 더 이상 early return 없음 (`canvas_screen.dart`)
5. **Rive 제거** — 빌드 에러 해결을 위해 rive 패키지 제거, `alien_character.dart`를 👽/🛸 이모지 `AnimatedSwitcher`로 대체
6. **README 현행화** — FastAPI 백엔드/Rive/분할 레이아웃 등 구형 정보 모두 제거, 현재 아키텍처 반영
7. **커밋 & 푸시** — `95936fa` → `origin/master` push 완료
</completed_work>

<remaining_work>

미완료 / 다음 세션 후보:
- **실기기 테스트** — APK 설치 후 실제 기기에서 확인 필요
  - 채팅 텍스트 줄바꿈이 정상인지
  - 그림(빨간 동그라미) 그린 채로 AI 질문 시 그림이 함께 전송되는지
  - 채팅창 크기/가시성이 개선됐는지
- **채팅창 UX 추가 개선 가능** — 현재 Column 세로 분할인데, 필요시 DraggableScrollableSheet로 교체 고려
- **AI 답변 품질** — "2-3문장" 제한이 실제로 잘 지켜지는지 확인
</remaining_work>

<decisions_made>

- **Rive 제거** — 네이티브 빌드 에러 해결을 위해 rive 제거, 이모지로 대체 (해커톤 범위 내)
- **세로 레이아웃 선택** — 모바일에서 좌우 분할보다 상하 분할이 채팅 가시성 훨씬 좋음
- **CourierPrime 제거** — 한국어 미지원 폰트 → 기본 시스템 폰트로 교체
</decisions_made>

<blockers>
- 없음. 모든 작업 정상 완료.
</blockers>

## Required Reading (in order)
1. `lib/features/canvas/presentation/canvas_screen.dart` — 레이아웃 구조 (Column 기반)
2. `lib/features/canvas/data/repositories/alien_repository.dart` — AI 시스템 프롬프트

## Infrastructure State
- Flutter 빌드: 정상 (`flutter build apk --debug` 성공)
- Git: `origin/master` 최신 (`95936fa`)
- `android/local.properties`: 커밋 제외 (빌드 자동 생성)
- `app-release.apk`: 루트에 존재 (gitignore 권장)

<context>
이번 세션은 사용자가 실기기 테스트 후 발견한 UI 문제들을 수정하는 세션이었음.
주요 문제: 텍스트 줄바꿈, 채팅창 크기, AI 텍스트 과다, 그림 미전송.
모두 수정 완료 후 빌드·커밋·푸시까지 마쳤음.
</context>

<next_action>
실기기에 APK 설치 후 테스트:
`adb install build/app/outputs/flutter-apk/app-debug.apk`
→ 이미지 열기 → 빨간 동그라미 그리기 → "탐사 질문하기" → AI 응답 확인 (짧은지, 그림 반영됐는지)
</next_action>
