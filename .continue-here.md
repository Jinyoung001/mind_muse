---
phase: hotfix — 외계인 조사관 API 연동 수정 + 보안 패치
task: 5/5 (완료)
status: paused
last_updated: 2026-04-27T09:47:33Z
---

<current_state>
이번 세션의 모든 작업 완료. 실기기 테스트만 남음.
APK: `build/app/outputs/flutter-apk/app-release.apk` (60.8MB)
</current_state>

<completed_work>

- Task 1: 외계인 조사관 `"error: the request connection took longer than 0:00.000..."` 에러 원인 분석 — Done
  - Dio 5.9.2가 SocketException을 `connectionTimeout(Duration.zero)`로 re-wrap하는 버그
  - 실제 원인: 백엔드 서버가 실행되지 않아 연결 실패
- Task 2: FastAPI 백엔드 제거 → `google_generative_ai` SDK 직접 호출로 교체 — Done (commit: 3fb160a)
  - `pubspec.yaml`: `dio` → `google_generative_ai: ^0.4.6`
  - `alien_repository.dart` 전면 재작성 (시스템 프롬프트 Dart 코드로 이전)
  - `test/alien_repository_test.dart` MockDio 기반 → SDK 방식으로 교체
- Task 3: GEMINI_API_KEY 유출 대응 — Done
  - `git filter-branch`로 전체 히스토리(57커밋)에서 `.env` 완전 삭제
  - `git push --force`로 GitHub 히스토리 덮어씀
  - `.env`가 `.gitignore`에 있으나 tracked 상태였던 것도 수정
- Task 4: 새 API 키 발급 후 `.env` 업데이트 — Done (로컬 전용, git 미추적)
- Task 5: APK 빌드 — Done (60.8MB)
  - 한글 경로 우회: `C:\mmu`로 복사 → 빌드 → APK 복사 → 임시폴더 삭제
</completed_work>

<remaining_work>

- Task 6: 실기기/에뮬레이터에서 외계인 조사관 최종 검증
  - APK 설치 후 그림 그리기 → 외계인 조사관 버튼 클릭
  - Gemini API 스트리밍 응답이 정상 표시되는지 확인
</remaining_work>

<decisions_made>

- **백엔드 완전 제거**: 앱 단독 실행을 위해 FastAPI 서버 의존성 없애고 Flutter에서 Gemini SDK 직접 호출
- **dio 제거**: HTTP 중간 레이어 불필요. `google_generative_ai: ^0.4.7` (실제 설치 버전)로 교체
- **API 키 보안**: git filter-branch + force push로 히스토리 재작성. 이후 `.env`는 절대 커밋 금지
- **APK 빌드 우회**: 한글 경로 문제는 ASCII 임시 경로(`C:\mmu`) 복사 방식으로 해결. 근본 해결은 프로젝트를 ASCII 경로로 이전
</decisions_made>

<blockers>

- **한글 경로 APK 빌드 문제**: `D:\00_개인프로젝트\...` 경로에서 Dart AOT gen_snapshot이 app.dill을 읽지 못함
  - 우회법: `C:\mmu`로 복사 후 빌드 (매번 수동 필요)
  - 근본 해결: 프로젝트를 `C:\projects\mind_muse` 같은 ASCII 경로로 이전
</blockers>

<context>
해커톤 앱 MindMuse의 핵심 기능인 외계인 조사관이 API 연동 오류로 동작하지 않아 긴급 수정.
백엔드 서버 없이 Flutter 앱이 Gemini API를 직접 호출하도록 아키텍처 변경 완료.
API 키 유출 사고도 처리 완료 (히스토리 삭제 + 새 키 발급).
현재 앱은 빌드 가능한 상태이며 새 APK가 생성되어 있음.
</context>

<next_action>
APK(`build/app/outputs/flutter-apk/app-release.apk`)를 실기기에 설치하고
외계인 조사관 버튼을 눌러 Gemini API 응답이 스트리밍되는지 확인.
문제 없으면 해커톤 제출 준비 완료.
</next_action>
